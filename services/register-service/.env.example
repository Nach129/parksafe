# Plantilla del register-service: copiar a .env para configurar registro de usuarios/vehiculos.
PORT=3001

# Conexion a la base de autenticacion compartida con login-service.
DB_HOST=localhost
DB_PORT=3306
DB_USER=ticketsafe
DB_PASSWORD=parksafe123
DB_NAME=ticketsafe_auth

# Rondas de bcrypt para proteger contrasenas antes de guardarlas.
BCRYPT_ROUNDS=10
